interface:
  display_name: "Vibe Coding Factory"
  short_description: "Gate lightweight cold-start side projects before code."
  default_prompt: "Use $vibe-coding-factory to turn my rough side-project idea into a monitored Direction Lock before any app code."

policy:
  allow_implicit_invocation: true
